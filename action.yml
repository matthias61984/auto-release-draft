name: 'Auto Release Drafty'
description: 'Drafts a GitHub release with the changes introduced by a newly created version tag.'
author: 'Corey Walsh'
outputs:
  release-url:
    description: 'The URL of the GitHub release that was drafted'
runs:
  using: 'node12'
  main: 'dist/index.js'
